/*
function start () {
    let submit = document.getElementById("submit");
    submit.addEventListener("click", toggle);
};

function toggle () {
    let color = document.getElementById(body);
    let backColor = color.style.backgroundColor;
    color.style.backgroundColor = backColor === "background-image" ? "white" : "background-image";
};

start();
*/